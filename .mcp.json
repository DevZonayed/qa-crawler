{
  "mcpServers": {
    "qa-crawler": {
      "command": "node",
      "args": ["./dist/index.js"],
      "description": "QA coverage crawler engine (build with `pnpm build` first). Use outside the plugin system, e.g. in another project's .mcp.json — point args at the absolute path to dist/index.js."
    }
  }
}
