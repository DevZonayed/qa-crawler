# pnpm 10+ blocks dependency build scripts by default and (since v11) reads the allow-list from THIS
# file, not package.json. better-sqlite3 must compile its native module and playwright must download
# Chromium, or the engine cannot run.
onlyBuiltDependencies:
  - better-sqlite3
  - playwright
